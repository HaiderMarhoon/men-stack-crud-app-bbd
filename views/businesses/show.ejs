<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>
        <%= findBusiness.name%>
    </title>
</head>

<body>
    <h1>
        <%= findBusiness.name %>
    </h1>

    <% if (findBusiness.isVerified) { %> ✅ Verified <% } %>

            <p><strong>Category:</strong>
                <%= findBusiness.category %>
            </p>

            <p><strong>Description:</strong></p>
            <p>
                <%= findBusiness.description %>
            </p>

            <p><strong>Location:</strong>
                <%= findBusiness.location %>
            </p>

            <p><strong>Phone:</strong>
                <%= findBusiness.phoneNumber %>
            </p>

            <% if (findBusiness.website) { %>
                <p>
                    <strong>Website:</strong>
                    <a href="<%= findBusiness.website %>" target="_blank">
                        <%= findBusiness.website %>
                    </a>
                </p>
                <% } %>
                    <div class="actions">
                        <form action="/businesses/<%=findBusiness._id%>?_method=DELETE" method="POST">
                            <button class="delete-button" type="submit">Delete <%= findBusiness.name %> </button>
                        </form>
                        <a class="edit-link" href="/businesses/<%= business._id %>/edit">
                            <div class="edit-text">Edit <%= business.name %>
                            </div>
                        </a>
                    </div>
                    <a class="businesses-index-link" href="/businesses">
                        <div class="index-link-text">Back to All Businesses</div>
                    </a>


</body>

</html>